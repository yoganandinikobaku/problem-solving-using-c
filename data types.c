#include<stdio.h>
int main()
{
  printf("size of int: %1u bytes\n",size of (int));
  printf("size of char: %1u bytes\n",size of (char));
  printf("size of float: %1u bytes\n",size of (float));
  return 0;
} 
